{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["dist/**", "shared/**"]
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server/vercel-server.js"
    },
    {
      "src": "/(.*)",
      "dest": "server/vercel-server.js"
    }
  ],
  "env": {
    "NODE_ENV": "production",
    "TS_NODE_COMPILER_OPTIONS": "{\"skipLibCheck\":true}"
  }
}